package codechallenges.linkedlist;

import datastructures.linkedlist.LinkedList;
import java.util.LinkedList;

public class LinkedListChallenges
{
  LinkedList firstList = new LinkedList<>();

  public void insert(){
    struct node *newNode = firstList(sizeof(struct node);
    newNode -> data = value;
    if(*head == null){
    *head = newNode;
    } else {
      struct node *lastNode =*head;
      while(lastNode = lastNode -> next;
    }
    lastNode -> next = newNode;
  }

firstList.addLast(&head, a);
firstList.addLast(&head, b);
firstList.addLast(&head, c);
  public static includes(str){
    firstList.contains(Object a);


    Return firstList;
  }

Public static toString(){
  return
  }
}


//**Retrieved from lo2base2.com
